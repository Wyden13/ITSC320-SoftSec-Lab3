# ============================================================
# Template for the real .env file. Copy to `.env` and fill in
# real values. `.env.example` is committed as documentation;
# `.env` (with real secrets) is git-ignored. See REPORT.md II-E.
# ============================================================

# AES key used to encrypt locally-stored notes. Use a long,
# random, 32-byte value in real deployments.
NOTES_ENC_KEY=

# Third-party API credentials.
API_KEY=
API_SECRET=
